## IMPORTANT Things Implemented

- _Due to webdriver js control flow pattern deprecation_ Use async/await or Promise chaining.
- _Page Object mechanism_ (deriving Class Objects which includes properties as an element finder type)
